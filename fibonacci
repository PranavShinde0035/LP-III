// DAA Practical 1 - Fibonacci Series (Recursive and Non-Recursive)
public class DAA1{
    // Non-recursive method
    static void fibonacciNonRecursive(int n) {
        int a = 0, b = 1, c;
        System.out.print(a + " " + b + " ");
        for (int i = 2; i < n; i++) {
            c = a + b;
            System.out.print(c + " ");
            a = b;
            b = c;
        }
        System.out.println();
    }

    // Recursive method
    static int fibonacciRecursive(int n) {
        if (n <= 1)
            return n;
        return fibonacciRecursive(n - 1) + fibonacciRecursive(n - 2);
    }

    public static void main(String[] args) {
        int n = 10;
        System.out.println("Non-Recursive Fibonacci:");
        fibonacciNonRecursive(n);

        System.out.println("\nRecursive Fibonacci:");
        for (int i = 0; i < n; i++)
            System.out.print(fibonacciRecursive(i) + " ");
    }
}

/*
Time Complexity:
Recursive: O(2^n)
Non-Recursive: O(n)
Space Complexity:
Recursive: O(n) (due to recursion stack)
Non-Recursive: O(1)
*/
